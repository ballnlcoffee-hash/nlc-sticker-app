# สเปกการสร้างวัตถุดิบหลัก / วัตถุดิบส่วนผสม / บรรจุภัณฑ์ (Master Item)

ที่มา: ฟีเจอร์ "วัตถุดิบ" (Master Item) ในแอป Barista sandbox — ครอบคลุมเฉพาะ 3 หมวดนี้ (ไม่รวม Prep Base ซึ่งเป็นฟีเจอร์แยกต่างหาก ดู `phase4-handoff-to-main-app.md`) สรุปกติกาการสร้าง/แก้ไข/ลบ + รูปแบบหน้าตา เพื่อนำไปเขียนโค้ดฟีเจอร์เดียวกันในแอปอื่นได้ตรงเจตนา

**อัปเดตล่าสุด**: วัตถุดิบหลัก / วัตถุดิบส่วนผสม / บรรจุภัณฑ์ **แยกห้องเป็นเมนูอิสระของตัวเองแล้ว** เลือกเข้าได้ตรงจากหน้าหลักเลย ไม่ต้องกดผ่านเมนู "Master Item"/"วัตถุดิบ" กลางแล้วเลือกแท็บย่อยเหมือนเดิมอีกต่อไป — **หลักการสร้าง/เงื่อนไข/ฟิลด์ทั้งหมดเหมือนเดิมทุกประการ** (ข้อ 1-8, 10 ด้านล่างไม่เปลี่ยน) เปลี่ยนแค่โครงสร้างการนำทาง/หน้าตาของหน้ารายการ (ข้อ 9)

---

## 1. แนวคิดหลัก

Master Item คือ "หน่วยวัตถุดิบที่ซื้อจริง" 1 รายการ แบ่งเป็น 3 หมวด (`type`) ที่แยกจากกันเด็ดขาด (ไม่ overlap กัน):

| type | ความหมาย | ตัวอย่าง |
|---|---|---|
| `วัตถุดิบหลัก` (MAIN_RAW) | วัตถุดิบตัวหลักของเมนู เลือกใช้แทนกันได้ในสูตร | เมล็ดกาแฟ, ผงชา, ผงโกโก้ |
| `วัตถุดิบส่วนผสม` (INGREDIENT) | ส่วนผสมที่ใส่เสมอ | นมสด, ไซรัป, น้ำ |
| `บรรจุภัณฑ์` (PACKAGING) | ภาชนะ/บรรจุภัณฑ์ | แก้ว, ฝา, หลอด |

อ้างอิงด้วย **SKU เป็นหลัก** (ไม่ใช่ชื่อ) — SKU เป็น stable identifier ที่ Recipe/Prep Base ใช้ผูกไว้ ห้ามเปลี่ยนแปลงหลังสร้างแล้ว

**ต้นทุนต่อหน่วยใช้งาน (`costPerUsageUnit`) ต้องเป็นค่าที่คำนวณเสมอ ห้ามให้ผู้ใช้กรอกตรงๆ**:
```
costPerUsageUnit = purchasePrice / quantityPerPurchaseUnit
```
เก็บค่าที่ไม่ปัดเศษ (full floating point) — ปัดเศษเฉพาะตอนแสดงผลเท่านั้น

## 2. ฟิลด์ของ Master Item

| ฟิลด์ | ชนิด | บังคับ | หมายเหตุ |
|---|---|---|---|
| `sku` | เช่น `MI-000001` | Auto-generate | รันตามลำดับ ห้ามผู้ใช้พิมพ์เอง, ใช้เป็น stable id ผูกกับ Recipe/Prep Base |
| `name` | ข้อความ | **บังคับ** | ห้ามว่าง |
| `type` | `วัตถุดิบหลัก` / `วัตถุดิบส่วนผสม` / `บรรจุภัณฑ์` | บังคับ | **กำหนดตายตัวโดยห้อง/เมนูที่เปิดฟอร์มอยู่** (ไม่ใช่ dropdown ให้เลือกในฟอร์มอีกต่อไป — ดูข้อ 9) รายการที่สร้างจากห้องไหนก็เป็น type นั้นเสมอ |
| `subType` | ดูตารางข้อ 3 | บังคับ (auto-เลือกตัวแรกของ type) | ขึ้นกับ `type` |
| `purchasePrice` | ตัวเลข > 0 | **บังคับ** | ราคาต่อ 1 หน่วยซื้อ |
| `purchaseUnit` | ข้อความอิสระ | **บังคับ** | เช่น kg, ลิตร, แพ็ค(50) |
| `quantityPerPurchaseUnit` | ตัวเลข > 0 | **บังคับ** | ปริมาณหน่วยใช้งานที่ได้จาก 1 หน่วยซื้อ เช่น 1000 (g ต่อ 1 kg) |
| `usageUnit` | ข้อความอิสระ | **บังคับ** | หน่วยที่ใช้อ้างอิงตอนใส่ในสูตร เช่น g, มล., ใบ |
| `costPerUsageUnit` | ตัวเลข (derived) | auto คำนวณ ห้ามกรอกตรง | ดูสูตรข้อ 1 |

## 3. ประเภทย่อย (subType) — ขึ้นกับ type ที่เลือก

| type | ตัวเลือก subType (พร้อมไอคอน) |
|---|---|
| วัตถุดิบหลัก | ☕ กาแฟ / 🍵 ชา / 🍫 โกโก้ / 🧂 ผง |
| วัตถุดิบส่วนผสม | 🥛 นม / 💧 น้ำ / 🍯 น้ำเชื่อม / 🥫 ซอส / 🧂 ผง / 🍒 ของแต่งแก้ว / 🧊 น้ำแข็ง |
| บรรจุภัณฑ์ | 🥤 แก้ว / 🔘 ฝา / 🧃 หลอด / 📄 กระดาษ |

เปลี่ยน `type` ในฟอร์ม → รายการ subType ต้อง refresh ใหม่ทันที และเลือกตัวแรกของ type ใหม่เป็นค่าเริ่มต้นเสมอ (กันค้าง subType ผิดหมวด)

## 4. เงื่อนไข/validation ตอนสร้าง-แก้ไข (ฟอร์ม)

ตรวจตามลำดับ พบ error แรกให้หยุดและแสดงข้อความนั้น (ไม่ต้องรวมทุก error พร้อมกัน):

1. `name` ห้ามว่าง (trim แล้วต้องไม่ว่าง) → "กรุณากรอกชื่อสินค้า"
2. `purchasePrice` ต้องเป็นตัวเลข > 0 → "กรุณากรอกราคาซื้อให้ถูกต้อง"
3. `purchaseUnit` ห้ามว่าง → "กรุณากรอกหน่วยซื้อ"
4. `quantityPerPurchaseUnit` ต้องเป็นตัวเลข > 0 → "กรุณากรอกจำนวนหน่วยใช้งานต่อหน่วยซื้อ"
5. `usageUnit` ห้ามว่าง → "กรุณากรอกหน่วยใช้งาน"
6. ระหว่างกรอก `purchasePrice`/`quantityPerPurchaseUnit` ต้องแสดง**ต้นทุนต่อหน่วยใช้งานแบบ live preview**ในฟอร์มทันที (ก่อนกดบันทึกด้วยซ้ำ) เพื่อให้ผู้ใช้เห็นผลลัพธ์ก่อนตัดสินใจบันทึก

แก้ไขรายการเดิม: **`sku` และ `type` ห้ามเปลี่ยนผ่านฟอร์มนี้** (type ถูกกำหนดตายตัวโดยห้องที่รายการนั้นสังกัดอยู่แล้ว — ดูข้อ 9) ฟิลด์อื่นที่เหลือแก้ไขได้อิสระ

## 5. สิทธิ์ (RBAC)

| การกระทำ | ADMIN | TRAINER | SALES |
|---|---|---|---|
| ดูรายการวัตถุดิบ | ✅ | ✅ | ✅ |
| สร้าง/แก้ไข/ลบ/สำเนา | ✅ | ✅ | ❌ (ดูอย่างเดียว) |
| เข้าศูนย์นำเข้าข้อมูล CSV | ✅ | ❌ | ❌ |

## 6. สำเนา (Duplicate)

กดสำเนาจากรายการใดก็ได้ → สร้างรายการใหม่ (`sku` ใหม่, auto) โดย copy `type`/`subType`/`purchasePrice`/`purchaseUnit`/`quantityPerPurchaseUnit`/`usageUnit` มาทั้งหมด ตั้งชื่อ = `"(copy)" + ชื่อเดิม` เพื่อให้เห็นชัดว่าเป็นสำเนาที่ต้องแก้ไขต่อ

## 7. ลบ (Delete)

- ลบแบบ **2-step confirm**: กดลบครั้งแรก → ปุ่มเปลี่ยนเป็น "ยืนยันลบ" (สีแดง) + ปุ่มยกเลิก, กดยืนยันอีกครั้งจึงลบจริง
- **⚠️ ข้อควรระวังสำหรับพอร์ตไปแอปจริง**: ระบบใน sandbox นี้เป็น **hard delete** (ลบออกจากฐานข้อมูลถาวร) และ**ไม่ได้เช็คว่ามี Recipe/Prep Base อ้างอิง SKU นี้อยู่หรือไม่ก่อนลบ** — ถ้าลบไปจะเกิด "SKU กำพร้า" ในสูตรที่เคยอ้างอิงไว้ (โชว์เป็น "ไม่พบวัตถุดิบ" ตอนคำนวณต้นทุน) จุดนี้เป็นจุดที่**ควรออกแบบใหม่**ตอนพอร์ต ไม่ใช่ copy behavior เดิมไปตรงๆ — แนะนำใช้ **soft-delete (สถานะ ACTIVE/INACTIVE) แทนการลบถาวร** เพื่อความปลอดภัยของข้อมูลที่ถูกอ้างอิงอยู่

## 8. นำเข้าวัตถุดิบจำนวนมากผ่าน CSV (Bulk Import)

แยก CSV ต่างไฟล์กันตามหมวด (MAIN/MIX/PACK คนละแบบฟอร์ม แต่คอลัมน์เหมือนกัน)

**คอลัมน์**: `name,sub_type,purchase_price,purchase_unit,quantity_per_purchase_unit,usage_unit`

- `name` ห้ามว่าง
- `sub_type` ต้องอยู่ในรายการที่ถูกต้องของหมวดนั้น (ดูข้อ 3)
- `purchase_price` ต้องเป็นตัวเลข > 0
- `purchase_unit` ห้ามว่าง
- `quantity_per_purchase_unit` ต้องเป็นตัวเลข > 0
- `usage_unit` ห้ามว่าง
- **ทุกแถวที่ผ่าน validate จะถูกสร้างเป็นรายการใหม่เสมอ ไม่ใช่การอัปเดตของเดิม** (แม้ชื่อจะซ้ำกับที่มีอยู่แล้วก็ยังสร้างใหม่ — แค่ขึ้น badge เตือน "ชื่อซ้ำกับวัตถุดิบที่มีอยู่" ให้ผู้ใช้ระวัง ไม่ได้บล็อกการ import)
- SKU ของแถวที่ import ถูก auto-generate ทั้งหมด ไม่ต้องมีคอลัมน์ sku ในไฟล์
- Flow บังคับ: Upload → Validate (list error รายแถว) → Preview (แสดงต้นทุนต่อหน่วยที่จะได้) → Commit

## 9. รูปแบบหน้าตา (UI Layout)

### การเข้าถึง (Navigation)
- **ไม่มีเมนูกลาง "Master Item"/"วัตถุดิบ" อีกต่อไป** — วัตถุดิบหลัก, วัตถุดิบส่วนผสม, บรรจุภัณฑ์ เป็น **3 เมนูแยกอิสระจากกัน** กดเข้าได้ตรงจากหน้าหลัก/bottom navigation เลย (คนละห้อง คนละหน้าจอ ไม่ได้ share list container เดียวกันแบบเดิม)
- แต่ละห้องเป็น "หน้ารายการ" สมบูรณ์ในตัวเอง (ตามโครงสร้างด้านล่าง) — ไม่มีแท็บสลับไปหมวดอื่นอยู่ในหน้าเดียวกันอีกแล้ว ต้องการดูอีกหมวดต้องกลับไปหน้าหลักแล้วเลือกเมนูใหม่
- ผลคือ**ไม่มีขั้นตอน "เลือกหมวดก่อน" อีกต่อไป** — เข้าห้องไหนก็สร้าง/แก้ไข/ดูเฉพาะของหมวดนั้นได้เลยทันที

### หน้ารายการ (List view) — โครงสร้างเดิมของแต่ละห้อง เหมือนกันทั้ง 3 หมวด ต่างกันแค่ข้อความ/ข้อมูลที่กรองอยู่
โครงสร้างบนลงล่าง:
1. **Header row**: หัวข้อ = ชื่อหมวดนั้นตรงๆ (เช่น "วัตถุดิบหลัก" / "วัตถุดิบส่วนผสม" / "บรรจุภัณฑ์" — ไม่ใช่คำกลางว่า "วัตถุดิบ" อีกแล้ว) + ปุ่มไอคอนวงกลม "⋯" เปิดตัวนำเข้าข้อมูล CSV **ของหมวดนั้นโดยตรง** (ขวา, แสดงเฉพาะ ADMIN) — กดแล้วเข้าฟอร์มนำเข้าของหมวดนี้ทันที ไม่ต้องผ่านหน้าเลือก "จะนำเข้าอะไร" อีกต่อไป (เพราะรู้อยู่แล้วว่าอยู่ห้องไหน)
2. **ช่องค้นหา**: ไอคอนแว่นขยายซ้ายในช่อง, placeholder "ค้นหาชื่อหรือรหัส" — ค้นหาจับคู่ทั้งชื่อและ SKU (case-insensitive), ค้นเฉพาะในหมวดของห้องนี้เท่านั้น
3. ~~แถบแท็บย่อยสลับหมวด~~ — **ตัดออก** (เดิมมีไว้สลับ MAIN/MIX/PREP/PACK ในหน้าเดียว ตอนนี้แต่ละหมวดแยกห้องแล้วจึงไม่จำเป็น)
4. **แถบตัวกรองประเภทย่อย** (icon chip, เลื่อนแนวนอนได้) — ยังอยู่เหมือนเดิม: เริ่มด้วย "◎ ทั้งหมด" ตามด้วยไอคอน+ชื่อ subType ทุกตัวของหมวดนี้ (ดูตัวเลือกในข้อ 3 ของเอกสารนี้)
5. **รายการการ์ด** (scroll แนวตั้ง): แต่ละใบแสดง
   - บรรทัดบน: ชื่อสินค้า (ตัวหนา) + SKU (ตัวเล็ก, monospace) ทางซ้าย, ต้นทุนต่อหน่วยใช้งาน (ตัวหนา) + "/ หน่วยใช้งาน" ทางขวา
   - badge ประเภทย่อย (ไอคอน+ชื่อ) ใต้ชื่อ
   - กดที่การ์ด → ขยาย/ยุบ แสดงรายละเอียด: ราคาซื้อ/หน่วยซื้อ, จำนวนหน่วยใช้งานต่อหน่วยซื้อ, และปุ่ม สำเนา/แก้ไข/ลบ (เฉพาะ ADMIN/TRAINER)
   - ไม่พบผลค้นหา → ข้อความกลางจอ "ไม่พบรายการที่ค้นหา"
6. **ปุ่มลอยด้านล่าง (FAB)**: ข้อความคงที่ตามหมวดของห้องนั้น เช่น ห้อง "วัตถุดิบหลัก" ปุ่มจะเป็น "+ เพิ่มวัตถุดิบหลัก" เสมอ (ไม่ต้อง derive จากแท็บที่เลือกอยู่แบบเดิมอีกแล้ว เพราะห้องนี้มีได้แค่หมวดเดียว) — ปิดใช้งาน (disabled) พร้อมข้อความ "🔒 ต้องเป็น Admin หรือ Trainer จึงจะเพิ่มรายการได้" ถ้า role เป็น Sales

### ฟอร์มเพิ่ม/แก้ไข (Bottom sheet)
เปิดจาก FAB หรือปุ่ม "แก้ไข" — เป็น modal แบบ bottom sheet เลื่อนขึ้นจากล่าง (`max-height:88vh`, เลื่อนดูได้) ลำดับฟิลด์บนลงล่าง:
1. ชื่อสินค้า (text input)
2. ~~ประเภท (dropdown 3 ตัวเลือก)~~ — **ตัดออก** เพราะตอนนี้ประเภทถูกกำหนดตายตัวโดยห้องที่เปิดฟอร์มอยู่แล้ว (เปิดฟอร์มจากห้อง "วัตถุดิบหลัก" ก็สร้าง/แก้เป็น `type: วัตถุดิบหลัก` เสมอ ไม่ต้องเลือกซ้ำ) — **⚠️ นัยที่ตามมา**: เดิม field นี้ยังใช้ตอนแก้ไขเพื่อ "ย้ายหมวด" ของรายการที่มีอยู่ได้ด้วย พอตัดออกแล้วต้องตัดสินใจว่าจะยังรองรับการย้ายรายการข้ามหมวด (เช่น จาก "วัตถุดิบส่วนผสม" ไป "วัตถุดิบหลัก") หรือไม่ — ถ้าต้องการ ต้องออกแบบเป็น action แยกต่างหาก (เช่น "ย้ายไปอีกห้อง") ไม่ใช่ dropdown ในฟอร์มแก้ไขแบบเดิม
3. ประเภทย่อย (dropdown, ตัวเลือกตายตัวตามหมวดของห้องนี้ — ดูตัวเลือกในข้อ 3 ของเอกสารนี้)
4. แถวคู่: ราคาซื้อ (บาท) | หน่วยซื้อ
5. แถวคู่: จำนวนหน่วยใช้งาน/หน่วยซื้อ | หน่วยใช้งาน
6. **กล่องแสดงผลคำนวณ**: "ต้นทุน/หน่วยใช้งาน" พร้อมค่าที่คำนวณสด อัปเดตทันทีที่แก้ราคาหรือจำนวน
7. แถบปุ่ม "ยกเลิก" / "บันทึก" — ติดขอบล่างของ sheet เสมอ (sticky) ไม่ให้คีย์บอร์ดบัง

## 10. อ้างอิง Enum ทั้งหมด

```
type:     วัตถุดิบหลัก (MAIN_RAW) | วัตถุดิบส่วนผสม (INGREDIENT) | บรรจุภัณฑ์ (PACKAGING)
subType (วัตถุดิบหลัก):     กาแฟ | ชา | โกโก้ | ผง
subType (วัตถุดิบส่วนผสม):  นม | น้ำ | น้ำเชื่อม | ซอส | ผง | ของแต่งแก้ว | น้ำแข็ง
subType (บรรจุภัณฑ์):       แก้ว | ฝา | หลอด | กระดาษ
```

## 11. ⚠️ ข้อควรระวังถ้าจะพอร์ตไปแอป Next.js/Firebase ที่ทำ Phase 1-3 ไว้แล้ว

แอปจริง (`internal-app`) มี Auto-SKU 3 prefix แยกตามหมวดอยู่แล้ว: `MAIN_RAW → "MAIN-000001"`, `INGREDIENT → "ING-000001"`, `PACKAGING → "PKG-000001"` (ดู `src/lib/master-items/sku.ts`) — sandbox นี้ใช้ `MI-000001` เดียวกันทุกหมวดเพื่อความง่ายตอนทดสอบเท่านั้น **อย่า copy SKU format นี้ไปแอปจริง** ให้ใช้ prefix ที่มีอยู่แล้ว และแอปจริงใช้ **soft-delete (ACTIVE/INACTIVE) เท่านั้น ไม่มี hard delete** อยู่แล้วตั้งแต่ Phase 2 — ตรงกับข้อเสนอแนะในข้อ 7 พอดี ไม่ต้องเปลี่ยนอะไรฝั่งนั้น
